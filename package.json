{
  "name": "agent-resource-scheduler",
  "description": "Dynamic resource allocation and scheduling system for long-running agent tasks, optimizing for compute cost and latency",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}